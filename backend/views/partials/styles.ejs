<!-- primary stylesheet, which is imported and used in every ejs view template file  -->
<style>
/* -------- GLOBAL styles ------------- */

html, body {
  display: flex;
  flex-flow: column nowrap;
  flex: 1 0 100%;
}

#login-form-segment {
  width: '45%';
}

.grid.container {
  margin-top: 5em;
}
.inputfile {
  width: 0.1px;
  height: 0.1px;
  opacity: 0;
  overflow: hidden;
  position: absolute;
  z-index: -1;
}

.listings-header { font-size: .75em; }

.centered-page {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  margin: 1em;
}

.centered-page .centered-header {
  text-align: center;
}

h1, h2, h3, h4, h5, h6 { text-align: center; }

html { scroll-behavior: smooth; }

.strong {
  font-weight: bold;
}

/* ---------- BANNER styles ------------- */
.banner-preview {
  width: 100%; 
  display: flex; 
  text-align: center; 
  justify-content: center; 
  align-items: center; 
  background: #E3E3D9; 
  min-height: 3em;
}

.banner-teal {
  background: #008a8f;
  color: #2e3238;
}

.banner-default {
  background: #85898e;
  color: #2e3238;
}

.banner-light {
  background: #F3F4F5;
  color: #2e3238;
}

.page {
  display: flex; 
  flex-direction: column; 
  align-items: center; 
  line-height: 1.5em; 
  margin-top: 3vh;
}

.banner-info {
  margin-top: 1.5em; 
  font-size: 1.15em; 
  color: #696969;
}

.form-container {
  max-width: 850px; 
  height: 50%; 
  display: flex; 
  flex-direction: column;
}

.container-content {
  padding: 0 .75em;
}

.textarea-div {
  margin-top: .75em; 
  margin-bottom: .75em;
}


.toggle-and-submit {
  display: flex; 
  flex-direction: column; 
  justify-content: center;
}

#html-hints-div p {
  margin: .25em 0;
}

/* ---------- NAVBAR styles ------------- */

.custom-right {
  display: flex; 
  justify-content: center;
}

.custom-right-menu {
  right: 0; 
  display: flex; 
  justify-content: flex-start;
}

/* ---------- DASHBOARD styles ------------- */

#dashboard .instructions {
  min-height: 45vh;
}

.hidden {
  display: none;
}

#dashboard main {
  display: flex; 
  flex-direction: column; 
  justify-content: flex-start;
  padding: 1em 2.5em;
  
}
#dashboard .steps {
  width: 300px;
}

/* Dashboard segment styles  */
.primary-content {
  display: flex; 
  flex-direction: column;
  justify-content: center; 
  align-items: center;
}

.primary-content details {
  margin-top: .5em;
}

/* ---------- LOGIN styles ------------- */

#login-page {
  display:flex;
  flex-direction: column;
  flex-wrap: nowrap;
  justify-content:center;
  align-items:center;
  background-color: #231935;
  text-align: center;
}

/* DESKTOP login form */
@media(min-width: 650px) {

    #login-segment {
      height: 350px;
      width: 70%;
      min-width: 600px;
      max-width: 650px;
      padding: 25px;
      display: flex;
      flex-direction: column;
      justify-content: space-evenly;
    }

    #login-page .divider {
      border-left: 2px solid #d9d9d9;
      height: 47%;
    }

    #login-page #dividers {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    #horizontal-divider {
      display: none;
    }

    #forms-div {
      display: flex;
      justify-content: space-around;
      height: 275px;
    }
    
}

/* MOBILE login form */
@media(max-width: 649px) {

  #login-segment {
      width: 90%;
      min-width: 250px;
      padding: 25px;
    }

    #login-page .divider {
      border-bottom: 2px solid #d9d9d9;
      width: 47%;
    }

    #horizontal-divider {
      visibility: visible;
    }

    #login-page #dividers {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    #forms-div {
      display: flex;
      flex-direction: column;
    }

}

#login-page form {
  color:#231935;
  padding:.5em;
}

#forms-div button {
  background-color:#4285F4;
}

#login-page .top-message {
  color:#d9d9d9;
}

#login-page .bottom-message {
  color:#d9d9d9;
  line-height:2em;
}

#login-page .bottom-message a {
  color:#4285F4;
  text-decoration: underline;
}

#divider-text {
  font-weight: 700;
  color: #231935;
  font-size: 1.15em;
  padding: 5px;
}

#google-form {
  display: flex;
  flex-direction: column;
  justify-content: center;
}

</style>